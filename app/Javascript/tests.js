/* Tests below are not proper tests, had difficulty implementing tests since javascript methods did not have return values */

describe('Animal Database Tests', function () {
  describe('playWithAnimal Test', function () {
    it('Level Up Animal when Experience is >= 100, then reset XP to 0, level up is called four times to reach level 1', function (){
      x = 0;
      lvl = 1;
    });
  });
  describe('Main Page runtime Test', function () {
    it('Main page should run within 10 ms', function (){
      return "You good bro";
    });
  });
});
